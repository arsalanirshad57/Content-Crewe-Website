import React from 'react'

const index = ({ className }) => {
    return (
        <div className="relative">
            <hr className={` w-60pr bg-gradient border-none h-1px ${className}`} />
        </div>
    )
}

export default index