import React from 'react'
import Span from '../Typography/Span'


const index = ({className}) => {
    return (
        <Span className={`!text-3xl text-gradient  my-auto ${className}`}>•</Span>
    )
}

export default index